# --- Confidentiality fence ---
# This repo is public. Private workbench material must never enter git
# history; canonical private copies live outside this repository (local-only).
tmp/
engineering-plan.md

# --- Node ---
node_modules/
dist/
coverage/
*.tsbuildinfo

# --- Env ---
.env
.env.*
!.env.example

# --- OS / editor ---
.DS_Store
*.swp
